{
  "buildCommand": "pnpm schema:validate && pnpm build:prod",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "env": {
    "NODE_ENV": "production",
    "DATABASE_PROVIDER": "postgresql"
  }
}
